# ATTRIBUTION

External libraries, models, and their licenses used by Nightingale (72h build).

## Backend (Python)

Declared in `backend/requirements.txt`:

| Package | License |
|---|---|
| FastAPI | MIT |
| Uvicorn | BSD-3-Clause |
| SQLAlchemy | MIT |
| Pydantic | MIT |
| pytest | MIT |
| httpx | BSD-3-Clause |

## Frontend (Node)

Declared in `frontend/package.json`:

| Package | License |
|---|---|
| react | MIT |
| react-dom | MIT |
| vite | MIT |
| typescript | Apache-2.0 |
| @vitejs/plugin-react | MIT |
| @types/react | MIT |
| @types/react-dom | MIT |

## Models / Providers

- Provider: DeepSeek (https://www.deepseek.com) via its anthropic-compatible endpoint
  (`https://api.deepseek.com/anthropic`), models `deepseek-v4-flash` / `deepseek-v4-pro`.
- Status: **NOT_LIVE_VERIFIED** — see `backend/docs/gate0_provider_status.md`.
  The live adapter is blocked; the build runs on `mock` / `deterministic_fallback`.
- No model weights are bundled. No provider API key is committed or logged.

## Notes

- The `anthropic` Python SDK was pip-installed ONLY for the one-off Gate 0 protocol
  probe; it is NOT a project dependency and is not in `requirements.txt`.
- Synthetic demo data is authored in-repo (`backend/seed/fixture.py`); no external
  dataset is used.
