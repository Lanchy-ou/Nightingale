# R4 — 管理员只管理账号与系统

优先级：P1。执行顺序：R1 → R2A → R3 → R4 → R5 → R2B → R6。

## 实施范围

移除默认临床读取；邀请改用患者 ID/姓名最小投影；检查可见 check-in 和管理输出。

## 验收与证据

从 backend 运行：

```powershell
.venv/Scripts/python.exe -m pytest tests/test_admin_no_clinical_read.py tests/test_patient_checkin_rbac.py tests/test_result_workflow.py
```

回归测试覆盖具体边界；最终集成结果、浏览器路径、性能报告及已知限制见
[验收记录](../boundary_acceptance_2026-09-08.md)。执行和数据恢复见
[操作说明](../boundary_repairs.md)。每项修复在最终集成状态统一验收；
提交顺序用于审阅变更，不代表已部署或已在工作数据库回填。

## 兼容与限制

不迁移 admin 为 clinician，不新增临床角色或临时授权；隐藏草稿继续使用既有 404 规则。
