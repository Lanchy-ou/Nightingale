# R3 — 独立持久化维护进程

优先级：P1。执行顺序：R1 → R2A → R3 → R4 → R5 → R2B → R6。

## 实施范围

Glance/任务 GET 移除升级提交；独立租约作业、退避、失败隔离；统一运行通知扫描。

## 验收与证据

从 backend 运行：

```powershell
.venv/Scripts/python.exe -m pytest tests/test_maintenance_boundaries.py tests/test_notification_delivery.py
```

回归测试覆盖具体边界；最终集成结果、浏览器路径、性能报告及已知限制见
[验收记录](../boundary_acceptance_2026-09-08.md)。执行和数据恢复见
[操作说明](../boundary_repairs.md)。每项修复在最终集成状态统一验收；
提交顺序用于审阅变更，不代表已部署或已在工作数据库回填。

## 兼容与限制

认证 last_seen 提交明确绕过业务通知扫描。后台停止时读取已有投影；启动 API 不再启动 worker。
